# ──────────────────────────────────────────────
#  AI-Assisted RLSE Trading Bot — Requirements
# ──────────────────────────────────────────────

# ── Core (existing) ──────────────────────────
MetaTrader5
Flask
Flask-Cors
pandas>=2.0
numpy>=1.24
pytz

# ── Data storage & I/O ───────────────────────
pyarrow          # parquet read/write
fastparquet      # alternative parquet engine

# ── Phase 1: Data pipeline ───────────────────
requests         # HTTP fetching (Binance, calendar)
tqdm             # progress bars for data downloads

# ── Phase 2–3: ML models ─────────────────────
scikit-learn>=1.3
lightgbm>=4.0
xgboost>=2.0
optuna>=3.0      # hyperparameter tuning
joblib           # model serialization
shap             # feature importance (optional)

# ── Phase 3–4: Visualization / Reports ───────
matplotlib
plotly

# ── Phase 5+: Deep learning (optional) ───────
# torch              # uncomment when needed for GRU/CNN
# torchvision        # uncomment if needed

# ── Testing ───────────────────────────────────
pytest
pytest-cov
